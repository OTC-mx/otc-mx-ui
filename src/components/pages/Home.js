import React from 'react';

const Home = () => (
  <div>
    <h1>OTC.mx</h1>
    <p>OTC.mx is a platform for trustlessly trading options on ERC20 tokens peer-to-peer.</p>
    <p>Read the <a href="/whitepaper">whitepaper</a> or get started using <a href="/call">call options</a> and <a href="/silentcall">silent call options</a>.</p>
  </div>
);

export default Home;
