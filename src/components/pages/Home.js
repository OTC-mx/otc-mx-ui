import React from 'react';

const Home = () => (
  <div>
    <h1>OTC.mx</h1>
    <p>OTC.mx is a platform for trustlessly trading peer-to-peer derivatives on ERC20 tokens.</p>
    <p>Read the <a href="/whitepaper">whitepaper</a> or get started using <a href="/call">call options</a> and <a href="/forward">forwards</a>. Or check out <a href="/silentcall">silent call options</a>, <a href="/tokenizedcall">tokenized call options</a>, and <a href="/managedforward">managed forwards</a>.</p>
    <p>The complete, unaudited contracts are live on Ethereum mainnet and Rinkeby testnet. Use at your own peril. ☠️‬</p>
  </div>
);

export default Home;
