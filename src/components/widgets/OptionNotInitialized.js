import React from 'react';

const OptionNotInitialized = () => (
  <div>
    <h2>Option not Initialized/Collateralized</h2>
    <p>You should probably fix this with the API or create a new one.</p>
  </div>
);

export default OptionNotInitialized;
