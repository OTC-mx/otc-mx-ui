import React from 'react';

const ContractNotInitialized = () => (
  <div>
    <h2>Contract not Initialized/Collateralized</h2>
    <p>You should probably fix this with the API or create a new one.</p>
  </div>
);

export default ContractNotInitialized;
